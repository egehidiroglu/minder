<h1>Let's setup the artists you'd like to follow! 🐱‍🏍</h1>

<div class="cards">
  <% @artists.each do |artist| %>
    <div>
      <div><%= artist.name %></div>
      <img src="<%= artist.poster_url %>" alt="Book poster"/>
    </div>
  <% end %>
</div>
