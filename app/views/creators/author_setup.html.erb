<h1>Let's setup the authors you'd like to follow! 🐱‍🏍</h1>

<div class="cards">
  <% @authors.each do |author| %>
    <div>
      <div><%= author.name %></div>
      <img src="<%= author.poster_url %>" alt="Book poster"/>
    </div>
  <% end %>
</div>
