<div class="container-creator-selection">
  <div class="titler d-flex justify-content-center">
    <h1>Let's choose the <span class="green-text">authors</span> you'd like to follow!</h1>
  </div>

  <div class="cards">
    <% @unfollowed_authors.each_with_index do |author, index| %>
      <div class="card-trip">
        <img src="<%= author.poster_url %>" alt="Album poster"/>
        <div class="card-trip-infos text-center">
          <%= author.name %>
          <%= form_with url:create_followed_creator_path, method: :post do |f| %>
          <%= f.text_field :creator_id, type: :hidden, value: author.id %>
            <%= f.submit '+', :class => 'btn-follow' %>
          <% end %>

        </div>
      </div>
    <% break if index == 7 %>
    <% end %>
  </div>
</div>

<div class="buttoner d-flex justify-content-end">

  <div class="bis">
    <%= link_to "Next", director_setup_path, class:"btn-ghoster mx-3 my-3" %>
  </div>
</div>
