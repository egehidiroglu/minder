<h1>Let's setup the directors you'd like to follow! 🐱‍🏍</h1>

<div class="cards">
  <% @directors.each do |director| %>
    <div>
      <div><%= director.name %></div>
      <img src="<%= director.poster_url %>" alt="Book poster"/>
    </div>
  <% end %>
</div>
