<div class="container">
  <h1>My Movies 🎬</h1>

  <% @movies.each do |movie| %>
    <% if current_user.creators.ids.include? movie.creator_id %>
      <ul>
        <li> <strong>Title: </strong><%= movie.name %></li>
        <div><strong>Release Date: </strong><%= movie.release_date %></div>
        <div> <strong>Director: </strong> <%= movie.creator.name %> </div>
        <div><%= movie.description %></div>
        <div><img src= "<%= movie.poster_url %>", alt="Book poster"></div>
      </ul>
    <% end %>
  <% end %>
</div>
